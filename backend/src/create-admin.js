const bcrypt=require("bcrypt");const{Pool}=require("pg");require("dotenv").config();
(async()=>{const name=String(process.env.ADMIN_NAME||"SAMSON").trim();const email=String(process.env.ADMIN_EMAIL||"").trim().toLowerCase();const password=String(process.env.ADMIN_PASSWORD||"");if(!email||password.length<12)throw new Error("ADMIN_EMAIL and a password of at least 12 characters are required.");const pool=new Pool({connectionString:process.env.DATABASE_URL});try{const hash=await bcrypt.hash(password,12);const{rows}=await pool.query(`INSERT INTO users(name,email,password_hash,role,is_active,failed_login_attempts,locked_until,password_changed_at) VALUES($1,$2,$3,'ADMIN',TRUE,0,NULL,NOW()) ON CONFLICT(email) DO UPDATE SET name=EXCLUDED.name,password_hash=EXCLUDED.password_hash,role='ADMIN',is_active=TRUE,failed_login_attempts=0,locked_until=NULL,password_changed_at=NOW(),updated_at=NOW() RETURNING id,name,email,role,is_active`,[name,email,hash]);console.log("Administrator created or reset successfully.",rows[0]);}finally{await pool.end();}})().catch(e=>{console.error(e);process.exit(1)});
